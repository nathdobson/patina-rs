use crate::Program;

pub struct Solve{
    program: Program,
    interval:f64
}

impl Solve{
    pub fn new(program: &Program, interval:f64) -> Solve{
        // program.diffentiate(1);
        todo!();
    }
}